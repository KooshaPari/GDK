# Research

## Codebase Findings

- `README.md` describes GDK as an enterprise git workflow system for AI agents.
- The README includes multi-agent workflow management, agent sessions,
  checkpoints, convergence, and agent recommendations.
- Existing stale badge evidence lives on `GDK-wtrees/sladge-badge` at commit
  `2a9daa8`, but that branch is behind the current active branch and the active
  README lacks the badge.

## Decision

GDK remains a direct Sladge target. The current branch needs a refreshed
README badge instead of relying on stale prepared evidence.
