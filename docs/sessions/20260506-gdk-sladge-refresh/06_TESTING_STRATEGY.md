# Testing Strategy

## Targeted Checks

- `git diff --check`
- `rg "sladge|AI Slop" README.md`
- `git status --short --branch`

## Deferred Checks

Broad Rust checks are not required for this badge-only documentation change and
are deferred because the local environment is currently low on disk.
