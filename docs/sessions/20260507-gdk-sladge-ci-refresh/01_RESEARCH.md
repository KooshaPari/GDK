# Research

## Local State

- Canonical `GDK` was clean on `ci/pin-trufflehog` at `aba116e`.
- Existing prepared branch `docs/gdk-sladge-current` was not a fast-forward
  from current canonical head: `git rev-list --left-right --count` reported
  `4 1`.
- Local `AGENTS.md` requires feature work under `repos/[PROJECT]-wtrees/<topic>/`
  and canonical integration only after validation.

## Sladge Target Rationale

The README describes GDK as a git workflow system for AI agents with
quality-based threading, convergence algorithms, and recommendations for agent
workflows, so the badge is appropriate governance disclosure.
